buildscript {
    repositories {
      mavenLocal()
    }
    dependencies {
        classpath "net.praqma:PackagePlugin:1.0.0"
    }
}
apply plugin: 'net.praqma.package'

buildproperties.publishing {
    zip {
        from ('build/resolvedDep/')
    }
}
